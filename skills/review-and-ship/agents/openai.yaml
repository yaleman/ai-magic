interface:
  display_name: "Review and Ship"
  short_description: "Implement, review, and open a pull request"
  default_prompt: "Use $review-and-ship to implement the requested change, clear all P0/P1 review findings, and create a pull request."
policy:
  allow_implicit_invocation: false
